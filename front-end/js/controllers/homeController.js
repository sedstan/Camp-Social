angular
.module('CampSocial')
.controller('homeController', homeController);

homeController.$inject=['User', 'TokenService', '$state', 'CurrentUser']
function hom (User,TokenService, $state, CurrentUser) {
  
}